# RAGE TRACKER - dependencias
opencv-python>=4.8
numpy>=1.24

# Micrófono (medidor de volumen / detección de gritos).
# Sin esto, la detección de gritos queda desactivada (la app sigue funcionando
# solo con emociones). En Linux instala también PortAudio: sudo apt install libportaudio2
sounddevice>=0.4.6

# Detección de insultos en tiempo real (Vosk STT - Spanish)
vosk>=0.3.45

# Alternativa a sounddevice (descomenta solo si no usas sounddevice):
# PyAudio>=0.2.13

# GUI nativa (opcional: si falta, cae a tkinter estándar, incluido en Python).
customtkinter>=5.2
